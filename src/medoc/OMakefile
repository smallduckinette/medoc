LDFLAGS += $(shell wx-config --libs) $(shell sane-config --ldflags) $(shell sane-config --libs) -lpqxx

FILES[] =
	$(removesuffix $(glob *.cpp))

CXXProgram(../bin/medoc, $(FILES))

.DEFAULT: ../bin/medoc$(EXE)
